let i = 1;

//precisa usar o = para atribuir um valor
i = i + 1;
// i += 1; isso aqui tem o mesmo significado do de cima
console.log(i);

// Essa lógica anterior se aplica a muitras operações aritmeticas
 i -= 1;
 i *= 2;
 i /= 2;
 i **= 3;
 i %= 2;

i++; // adiciona mais 1 na variavel = i = i + 1
i--; // mesma lógica 

++1; // muda so o console, pois a linguagem faz em ordem, entao no i++ ela vai primeiro
     // printar e depois q faz a operação, no caso do ++1 eçle faz a opeçsao dps printa o resultado
